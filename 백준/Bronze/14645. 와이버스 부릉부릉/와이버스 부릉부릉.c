#include <stdio.h>

int main() {

    int N, K;
    int N1, K1;
    
    scanf("%d %d", &N, &K);
    
    for (int i = 0; i < N; i++) {
        scanf("%d %d", &N1, &K1);
    }
    printf("비와이");

    return 0;

}